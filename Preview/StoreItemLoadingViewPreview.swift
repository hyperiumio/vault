#if DEBUG
import SwiftUI
/*
struct StoreItemLoadingViewPreview: PreviewProvider {
    
    static var previews: some View {
        NavigationView {
           StoreItemLoadingView(state)
        }
        .preferredColorScheme(.light)
        .previewLayout(.sizeThatFits)
        
        NavigationView {
           StoreItemLoadingView(state)
        }
        .preferredColorScheme(.dark)
        .previewLayout(.sizeThatFits)
    }
    
}
 */
#endif
