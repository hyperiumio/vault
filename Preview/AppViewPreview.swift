#if DEBUG
/*
import Preferences
import SwiftUI

public actor DefaultsServiceStub: DefaultsService {
    
    public let defaults = Defaults(isBiometricUnlockEnabled: false, activeStoreID: nil)
    
    public func set(isBiometricUnlockEnabled: Bool) async {}
    public func set(activeStoreID: UUID) async {}
    
    static let shared = DefaultsServiceStub()
    
}

struct AppViewPreview: PreviewProvider {
    
    static let state = AppState(defaultsService: DefaultsServiceStub.shared)
    
    static var previews: some View {
        AppView(state)
            .preferredColorScheme(.light)
            .previewLayout(.sizeThatFits)
    }
    
}
 */
#endif
