import XCTest
@testable import Crypto

/*
class UnsignedInteger32BitCodingTests: XCTestCase {
    
    func testEncodeUInt32() {
        let data = UnsignedInteger32BitEncode(67305985 as UInt32)
        
        XCTAssertEqual(data, "01020304")
    }
    
    func testEncodeInt() {
        let data = UnsignedInteger32BitEncode(67305985 as Int)
        
        XCTAssertEqual(data, "01020304")
    }
    
    func testDecodeUInt32() {
        let value = UnsignedInteger32BitDecode("01020304") as UInt32
        
        XCTAssertEqual(value, 67305985)
    }
    
    func testDecodeUInt32SliceIndependent() {
        let dataChunk = Data("FF01020304")
        let data = dataChunk[1...]
        let value = UnsignedInteger32BitDecode(data) as UInt32
        
        XCTAssertEqual(value, 67305985)
    }
    
    func testDecodeInt() {
        let value = UnsignedInteger32BitDecode("01020304") as Int
        
        XCTAssertEqual(value, 67305985)
    }
    
    func testDecodeIntSliceIndependent() {
        let dataChunk = Data("FF01020304")
        let data = dataChunk[1...]
        let value = UnsignedInteger32BitDecode(data) as UInt32
        
        XCTAssertEqual(value, 67305985)
    }
    
}
*/
