import Foundation

struct AppServiceStoreInfo {
    
    let created: Date
    let bankAccountItemCount: Int
    let bankCardItemCount: Int
    let customItemCount: Int
    let fileItemCount: Int
    let loginItemCount: Int
    let noteItemCount: Int
    let passwordItemCount: Int
    let wifiItemCount: Int
    
}
